#pragma once
#include <string>
#include <vector>

using namespace std;

// 최소직사각형
class minSquare {
public:
    int solution(vector<vector<int>> sizes);
};

// 나머지가 1이 되는 수 찾기
class mod1 {
public:
    int solution(int n);
};